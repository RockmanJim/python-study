f.seek()
如果要改变文件当前的位置, 可以使用 f.seek(offset, from_what)函数，返回值为位置
from_what：0开头, 1当前位置, 2结尾，例如：
seek(x,0)：从起始位置即文件首行首字符开始移动x个字符
seek(x,1)：表示从当前位置往后移动x个字符
seek(-x,2)：表示从文件的结尾往前移动x个字符
（汉字算俩字节，移动到汉字的一半无法读取）

f.tell()
返回文件对象当前所处的位置，是从文件开头开始算起的字节数

f的类型：_io.TextIOWrapper