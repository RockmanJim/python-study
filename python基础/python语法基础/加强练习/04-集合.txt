集合支持增删改查，其中数据不会重复{}
扩展：
列表支持增删改查，其中数据可以重复[]
元组只支持查询，其中数据可以重复()

以往需要获取一个不含重复元素的列表，可以用如下方式
a = [11, 22, 33, 11, 22, 33]
b = []
for i in a:
	if i not in b:
		b.append(i)
		
		
用set（集合），就可以比较方便地获取了
f = set(a)
b = list(f)
